'use strict';

var through = require('through2');
var path = require('path');
var gutil = require('gulp-util');
var PluginError = gutil.PluginError;
var File = gutil.File;

function rollItUp(config) {
	
}

module.exports = function gulpRollupInline(rollupConfig, baseDir) {

};

module.exports.watch = function gulpRollupInlineWatch(rollupConfig, baseDir) {

};
